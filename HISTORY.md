# 開発履歴

## 発想レベル拡張（2026-08-11）— `--idea-level` standard/very/extreme の3段化

ユーザー指示（2026-08-11）「発想の探索パラメータを 3 段階に: ①一般的に極端 0.9 / ②非常に極端 1.2 / ③極度に極端 1.5」に応え、従来の単一温度 0.9 を発想レベル 3 段に拡張した。計画は `資料/温度パラメータ/発想レベル拡張計画.md` に先行作成。

### 調査の答え（温度1越えの可否と適切値）

- **温度 1 越えは設定可能。** 実API呼び出しで DeepSeek Anthropic 互換ゲートウェイが温度 1.2・1.5 をエラーなく受理することを確認した（「温度の1越え設定の可否」→ 可）。
- **reasoning モデルでは温度単独は確実なレバーにならない**（「どの程度のパラメータが適切か」→ ヒントを主レバーにした 2 レバー設計）。単一サンプルでは温度だけでの「より極端」を証明できず、意味論はプロンプトの**発散ヒント**が担い、温度はサンプリングの補強に使う。値は 0.9/1.2/1.5 の単調な梯子。1.5 超は支離滅裂リスク、0.9 は後方互換を保つ。

### 実装

- **`IDEA_LEVELS` 表**（`elevate/engine.py`）: standard 0.9 / very 1.2 / extreme 1.5。温度はエンジンが所有、ヒントキーは i18n が所有（`adapter_hint()`）。
- **適用範囲**: 発散（diverge）と昇華推理（Aufheben）に同一レベルで適用。**最終化はレベルに関わらず常に 0.0 固定**。
- **`sdk` アダプタ**: 温度とヒントの両方を API に渡す。`idea_level` **明示指定時のみ**ヒント注入（既存の温度のみの呼び出しは従来挙動を維持＝後方互換）。
- **`claude-code` アダプタ**: 温度つまみがないためヒントのみで近似。3 段ヒント（①②③）＋未指定時は従来の温度ベース 2 段フォールバック。
- **CLI**: `--idea-level {standard,very,extreme}`（既定 standard）。`draft_temperature` パラメータは後方互換で維持。
- **ドキュメント**: CLAUDE*.md 固定方針・README 3言語・`docs/idea-levels.md`（en/ja/zh）を更新。
- **実行時パラメータの保存（`parameters.md`）**: ユーザー指示「出力ファイルのどこかに実行時のパラメータを記載」に応え、`--out` 保存時に input.md / format.md と並列の `parameters.md` にコマンド・言語・エンジン・方式・発想レベル（温度付き）・エージェント・出力先・出力形式・前提知識の有無等を構造化して記録（compare は evaluate/runs/baseline、improve は rounds 等も）。3言語ロケール対応。
- テスト **259 件パス**（ヒント注入・温度マッピング・後方互換・CLI 受理/拒否・parameters.md 保存）。

### 行動較正と実APIサンプル（実測完了）

同一タスク（通勤廃止）を ①standard / ②very / ③extreme で**実API昇華**し、`examples/idea-levels-ja/{standard,very,extreme}/` に保存（各レベルの input/format/drafts/artifacts/parameters.md が独立した完全サンプル）。実行は `examples/_run_idea_levels.sh`。

**結果（n=1 の質的観察）**: レベルを上げるほど破壊対象の階層が一段ずつ深くなり、2レバー設計（ヒント主＋温度補助）で段差が出力に現れた。

| レベル | 温度 | 昇華成果物の核心 | 破壊する前提の階層 |
|---|---|---|---|
| ① standard | 0.9 | **都市到来法**（一条を企業法・労働法・都市計画・税制に貫く） | 通勤を生む**制度的前提**を法律で消滅させる |
| ② very | 1.2 | **時間的位相遷移プロトコル**（移動を時間的位相遷移へ置換、複数の時間的自己を法承認） | 「**移動**」という概念そのものを消去 |
| ③ extreme | 1.5 | **マクロ量子遍在身体**（全人類の素粒子を量子もつれへ。「左手から右手へ通勤する」の無意味さ） | 「**身体は時空の一点に局在する**」という存在論的前提を爆破 |

- strategist 草案でも段差: ①「賃金と引き換えに売る物理的拘束」→ ②「「人を動かす経済」の消滅」→ ③「タイムトラベルで『通勤』という語を全言語から抹消」。
- 温度 1.2 / 1.5 はゲートウェイがエラーなく受理し完走（温度1越え設定の可否→**可**の再確認）。
- n=1 のため統計的優位性は主張しない。質的観察の詳細は `examples/idea-levels-ja/README.md`。

## 先行多言語化への追いつき（2026-08-11）— SKILL.md・install.sh・エージェント pre-link

ユーザー指示（2026-08-11）「wisdom-council-layer は先行して多言語化している。差分として追いついていない部分を修正して」に応え、`資料/多言語化/claude-code-plugin-i18n-pattern.md` の横展開チェックリストで両リポジトリを照合した。核の i18n（locales/・prompts/・agents/・docs ミラー・--lang 解決・plugin.json languages・較正）は先行側に追いつき済みであることを確認し、**残っていた3差分を修正**した。

- **SKILL.md を英語化＋Language Mode 節を追加**: 本体が日本語中心（ZH 未対応）だったのを英語化し、wisdom-council の SKILL.md と同じ「単一ファイル言語切替」方式に揃えた。`lang` 解決（`$ARGUMENTS.lang` > `ELEVATE_DRAFT_ENGINE_LANG` > `en`）・suffix 対応・`--agents` が言語非依存ベース名であること・**ユーザー向け出力（保存成果物と報告）は要求言語で書く**指示を明記。Prompt 内の Step0 リポジトリ紹介は従来**全ユーザーに日本語ハードコード**だったのを、要求言語で提示する指示に変更。Step5 報告にも要求言語を明記。
- **install.sh の出力メッセージを英語化**: 完了時 echo（143-146行）の日本語コメント（「ファサード（main.py を起動）」「クリエイターエージェント（サブエージェントとして起動可）」等）を英語化。インストール対象（全24エージェント）は従来どおり言語非依存で全バリアントを扱う。
- **`.claude/agents/` に -ja/-zh symlink を追加**: 従来 EN 8種のみ pre-link だったのを、wisdom-council（install.sh 再実行で全30種リンク済み）と同様に **24種（8×3）全対応**へ。symlink 解決確認済み（broken 0）。
- テスト 246 件パス（`pytest tests/`）。SKILL.md / install.sh / symlink は既存テストに依存なし。

## 実測記録（2026-08-11）— 素AI比較・複数ループ・知識注入の多言語実検証

前節（i18n 完了）の残タスクとして、en/ja/zh の3言語で**実API検証**を完遂した（mock ではなくゲートウェイ実呼び出し）。結果はすべて `examples/i18n/` に保存済み。

### 素AI比較（compare --evaluate --runs 1）— ELEVATE が全言語で素の生成を上回る

タスク「朝のルーティーン設計」を3言語で実生成し、素の単発生成 vs 昇華（弁証法的止揚）を5軸+品質評価で比較。

| 言語 | 素の生成 | ELEVATE | 差 | 素の生成の品質 | ELEVATE の品質 |
|---|---|---|---|---|---|
| ja | 0.221（⚠定番） | 0.462 | **+0.241** | 新奇0.10/独自0.20/意外0.10 | 新奇0.60/独自0.70/意外0.50 |
| en | 0.203（⚠generic） | 0.450 | **+0.247** | 0.15/0.15/0.10 | 0.30/0.70/0.40 |
| zh | 0.292（⚠定番） | 0.597 | **+0.305** | 0.20/0.20/0.20 | 0.80/0.70/0.60 |

- 全言語で ELEVATE が **+0.24〜+0.31** 上回り、素の生成はすべて「定番/generic」、ELEVATE は「独自概念（threshold act・夜慣性・主权回归・少而准 等）を伴う差別化」判定。
- 生成物は `examples/i18n/morning-compare-{en,ja,zh}/` に保存（input/format/drafts/artifacts/{raw,elevated,reconciliation}.md）。
- `--runs 1` は統合レポート（compare_report.md / measurement.md）を保存しない仕様のため、スコアは本節に記録した。

### ベースライン安全弁の欠如を修正（素の生成の完全性ガード）

初回実測で **en・zh の compare が落ちた**（ja のみ成功）: 素の生成が抽出フォーマットの max（en 2500 / zh 1500）を超える過長出力（en 5041字 / zh 2870字）を出し、`_baseline_is_complete` の出力量過多判定が不完全扱い → temperature 0.0（決定論的）で3回とも同一失敗 → RuntimeError で compare 全体が中断。

- 自己矛盾仕様の安全弁（`fallback_is_complete`）が **昇華側にのみあり、素の生成側に欠如** していた不整合が根本原因（en/zh は max 1500〜2500 なのに finalize_guidance が詳細な多節構成を要求＝実質達成不能）。
- 修正: `elevate/engine.py` の `Engine.generate` に `_raw_fallback_is_complete` を追加。汚染出力は安全弁でも拒否（汚染保護を弱めない）・fmt の output_is_direct は尊重・文終端で受容する。昇華側と対称に、上限回数試行後に構造的完成出力を安全弁で受容して続行する。
- 回帰テスト `test_generate_baseline_safety_valve_accepts_overmax_complete` 追加（過長だが完成 → 受容 / 汚染+過長 → 明示的失敗）。テスト **242 件パス**。
- 修正後、en/zh compare は exit 0 で完走（素の生成・最終分析とも安全弁「受け入れて続行」が動作）。
- **en の storyteller 草案は初回・再実行とも失敗・スキップ**（草案ガードの1000字上限超過。per-agent 捕捉の設計どおりで、他3エージェントの弁証法は継続して完走）。

### 複数ループ（improve）の多言語実検証

`morning-improve-{en,ja,zh}/` に保存。高品位停止（--quality-ceiling 既定0.85・既定有効）の多言語動作を確認。

| 言語 | round1 | round2 | 改善 | 結果 |
|---|---|---|---|---|
| ja | 0.593 | 0.622 | **+0.030** | 改善、継続 |
| en | 0.647 | 0.451 | -0.196 | 早期停止（回帰を防ぐ） |
| zh | 0.711 | 0.675 | -0.036 | 早期停止（回帰を防ぐ） |

### 知識注入の多言語実検証

`tagline-{en,ja,zh}/` に保存。キャッチコピー「履くたび、海が軽くなる。」/ "Maru: Every step returns to the sea." / 「海跑成圆。」— 前提知識（再生PET・環境負荷70%削減・20〜30代都市部）を捏造なく反映し、形式検出も言語適応（ja: コピー+選定理由200-800字 / en: 単行タグライン10-200字 / zh: 標語+理由）。

### 行列統合のための改修（--runs 1 の測定・評価記録保存）

ユーザー指示（2026-08-11）「評価ファイルも保存」「多言語 compare を行列に統合」に応え、単一 run でも記録を残すようにした。

- **main.py**: `--runs 1 --evaluate --out` でも (1) `evaluations/evaluation_baseline.md`・`evaluation_elevated.md` を保存、(2) `measurement.md` を保存する。n=1 は 95%CI・効果量が意味を持たないため省略し「n=1（単一 run）のため 95%CI・効果量は省略。」を注記（locales/ 3言語に `measurement_single_run_note` 追加）。
- **check_matrix_progress.py**: measurement.md の差/勝率ラベルを ja/en/zh の3言語でパース（言語別ラベル対応）、走査範囲を `examples/i18n/*/measurement.md` に拡張。
- **行列の再登録（2026-08-11）**: ユーザー承認により en/ja/zh の morning-routine（各 n=1）を行列に統合。累積勝率・平均差のみで規則を適用（n=1 のため CI は使わない。独立run前提の統計とは別物）。再実行後の最終測定: ja +0.334 / en +0.312 / zh +0.367、**累積勝率 100%・平均差 +0.338 → 継続判定**（打ち切りにならない）。
- テスト: 246 件パス（`pytest tests/`）。回帰テスト `test_compare_single_run_saves_evaluations_and_measurement` + `test_matrix_progress.py`（多言語ラベル・i18n 走査・継続判定）を追加。


## i18n 多言語化完了（2026-08-10）— en/ja/zh で全機能を回す

`資料/多言語化ver2/elevate-draft-engine-i18n-plan.md`（ver2 計画）を実施し、en/ja/zh の3言語対応を完了した。
wisdom-council-layer（ver1）と同じ3層方式（locales/・prompts/・agents/ 言語別）を、Python エンジン構成に適用。

- **言語解決**: CLI `--lang` > `ELEVATE_DRAFT_ENGINE_LANG` env > 既定 en（D1）。`load_agents(lang=...)` は接尾辞を剥いてベース名8体を返すため `--agents` は言語非依存。conftest が env を ja に固定し既存 ja テストを保護。
- **品質キー英語化（D2）**: 品質評価 JSON を `novelty`/`originality`/`surprise`/`rationale` に一本化。
- **docs ミラーツリー**: README/CLAUDE/docs/examples-README を en メイン + ja/zh ミラーへ。en/zh ドキュメントの日本語漏れを除去（grep スキャンで確認）。
- **examples/i18n 実生成反映**: `morning-routine-{en,ja,zh}` の3サンプルを、mock テンプレートから**実回答**へ書き換え（同一タスク「朝のルーティーン設計」を3言語で実API生成）。format.md も各言語に追加。
- **全コマンド × 3言語のカバレッジテスト**: generate/diverge/synthesize + `--agents`/`--knowledge-file`/`--output-format`・compare `--runs`・improve `--rounds 2 --evaluate` を test_i18n.py に追加。テスト 241 件パス（内 28 件 test_i18n.py）。

### 空応答の根本原因と修正（max_tokens 拡大）

en の止揚（aufheben）が**12連続で空応答**になり、en 実生成が完了できない障害が発生（ja/zh は成功）。診断の結果:

- 実ゲートウェイ（api.deepseek.com/anthropic）は reasoning モデルが **thinking（思考）ブロック**を返す。
- thinking と text は **max_tokens を共有**し、長い system（en の AUFHEBEN_SYSTEM 670字 + 「Prioritize depth」）だと思考が 4096 を全て消費し、text が 0 の「空応答」になる（診断: `stop_reason='max_tokens'` + thinking のみ）。
- ja（277字）/ zh（221字）は system が短く思考も短いため 4096 でも成功。en だけ失敗するのは system 長（思考量）の差で、入力草案サイズや並列とは無関係。

**修正**: `adapters/claude_client.py` の `max_tokens` 既定を 4096 → **16384** に引き上げ（`CLAUDE_MAX_TOKENS` env で変更可。README の env 表に追記）。回帰テスト3件追加（思考込みの予算を保証）。修正後、en 止揚は 18KB 級の完全な弁証法的昇華を生成して成功（実証済み）。

### ランタイム較正（フェーズ8.2）— en バイアスは再現せず

`utils/calibrate_language_bias.py` で同一成果物（en 昇華版）を en/ja/zh で各3回評価。

| 指標 | en | ja | zh |
|---|---|---|---|
| 5軸平均 | 0.717 [0.667, 0.766] | 0.750 [0.728, 0.772] | 0.727 [0.709, 0.744] |
| 品質平均 | 0.472 [0.396, 0.548] | 0.378 [0.318, 0.437] | 0.544 [0.402, 0.687] |
| overall | 0.433 (below) | 0.400 (below) | 0.478 (below) |

**所見**: (1) ver1（wisdom-council）の「en が系統的に +8〜9 点」は**再現しない**。品質平均で ja -0.094・zh +0.072 と方向が混在 = 系統的バイアスでない。(2) 5軸平均のずれは ±0.03 以内で計画の目安 0.05 未満 → **閾値の再取得は不要**。(3) 品質のずれは n=3 で CI が重なり統計的に分離できず、ノイズの可能性（要望があれば repeats 増で再測定）。(4) PASS/FAIL 判定は言語で反転しない（全言語 below で一貫）。

- テスト: 241 件パス（`pytest tests/`）。知識・複数ループ・多言語の相互検証を追加（i18n+knowledge 73件、compare/improve 3言語分）。
- ついでに修正: `資料/`（設計資料・較正レポート）をコミット対象外として .gitignore に追加。

## 文書整理（2026-08-09）— README を概要化し docs/ に分離

README の深掘りセクション（出力フォーマット認識・前提知識注入・compare/improve・Python API）を
`docs/` に切り出し、README は各節を概要＋リンクに圧縮（456行→295行）。あわせて内部の重複
（昇華優位性の設計目標の二重説明、品質評価節と実測記録との実測値重複）を解消し、
実測の実績は「実測記録」セクション一箇所に一元管理した。CLAUDE.md の文書ルールに `docs/` を追記。

## 2.3.0 (2026-08-09) — 品質評価（定番さ・独自性）を overall に統合

**問題**: 5軸評価（多様性・統合性・超越性・誠実性・実用性）は「定番さ・独自性」を測らない。
素の生成（指示のみ）が定番タスクで無難な回答を出しても、枠組みの多様さ・誠実さ・実用性で
高得点になり、独自性の差が overall に反映されない。実測では素の生成（定番回答）と昇華
（独自回答）の5軸 overall 差は +0.03 に留まり、「視野が狭い素の生成」が評価上ほぼ同点になる。

**対処**: 品質評価（新奇度・独自性・意外性）を `evaluation/quality.py` として追加し、
EvaluationEngine に統合。overall を掛け算方式で算出する。

    overall = 5軸overall × (α + (1−α) × 品質スコア)     α = 0.25
    品質スコア = (新奇度 + 独自性 + 意外性) / 3

定番回答（品質スコア低）ほど係数が下がり overall が減る。

- 経緯: 「サブ評価軸」として 5軸と独立の補助指標に試作 → ユーザー指示で **overall に掛け算統合**（α=0.25「サブ強め」）→ 「サブ評価」という名称を廃止し、評価体系全体を **「品質評価」** に再編 → 「定番度」を測って反転する二段構えをやめ、評価者に **新奇度を直接測らせる**（全観点「高いほど良い」で一貫。「定番度」という用語は完全に廃止）。
- `--quality`（既定）/ `--no-quality`（5軸のみ）で統合を切替え可能。mock では品質評価なし。
- **しきい値を新スケールに再調整**: Pass しきい値 0.70→**0.60**、improve の高品位停止 `--quality-ceiling` 0.85→**0.75**（overall の絶対値が掛け算で下がるため。実測の優秀な昇華版 0.67 前後が Pass になる水準）。
- 実測（compare-morning 成果物）: 素の生成（定番、新奇度0.1-0.2）overall 0.23〜0.28 vs 昇華（独自、新奇度0.9）0.58〜0.67。差が 5軸単独の +0.03 から **+0.4 前後**に拡大。
- テスト: test_subaxes.py → test_quality.py に置き換え。テスト182件パス。
- ついでに修正: `--runs` のヘルプ文字列の「95%信頼区間」が argparse の % フォーマットと衝突して
  `compare --help` 等が ValueError で落ちるバグを修正（`%` → `%%`。全サブコマンドの --help を確認）。

## 実測記録（2026-08-09）— multi-domain 素の生成 vs 既存の昇華

`examples/multi-domain/` の6分野について、素の生成（単発・**フォーマットなし**・知識あり＝タスク＋【前提知識】のみ）
を新規に生成し、既存の昇華版（`artifacts/elevated.md`）と現在の評価器（5軸 × 品質、α=0.25）で比較した。

| ケース | 素の生成 | 既存の昇華 | 差 | 勝敗 |
|---|---|---|---|---|
| lyrics（星） | 0.282 | 0.414 | +0.132 | 昇華 |
| business-plan | 0.621 | 0.706 | +0.084 | 昇華 |
| haiku | 0.294 | 0.638 | +0.343 | 昇華 |
| tagline | 0.324 | 0.529 | +0.205 | 昇華 |
| press-release | 0.268 | 0.525 | +0.257 | 昇華 |
| abstract | 0.251 | 0.638 | +0.387 | 昇華 |

6ケース: **昇華6勝・素の生成0勝、平均 0.340 vs 0.575（差 +0.235）**。

- 昇華優位はこの実測では**全ケースで確認**。品質評価が大きく効いた——昇華版は品質 0.83〜0.90 が大半で、
  素の生成はほぼ定番（0.13〜0.30）。lyrics（星）の昇華版のみ品質 0.47（新奇度0.40）と控えめだった。
- **lyrics はテーマを「海」→「星（距離・憧憬）」へ変更して再実行**（2026-08-09、ユーザー指示）。
  旧テーマ（海）では素の生成 0.613 vs 昇華 0.594 と唯一の素の生成勝利だったが、星テーマでは
  昇華 0.414 が素の生成 0.282 に勝利（+0.132）。昇華版は「届かない→届いている」の肯定転換、
  「距離は二つの光のあいだ」等で独自性が評価された。
- **汚染事象と対策**: press-release の初回素の生成が「了解しました。**Elevate-Draft-Engine** を起動します」という
  ファサード skill の起動文言を出力し、プレスリリース本体を書かなかった。原因は `~/.claude/skills/` に
  グローバル設置された elevate-draft-engine skill が `claude -p` の素の生成に干渉したため（素の生成は
  中性ディレクトリ起動だが、グローバル skill は cwd に関わらず読まれる）。→ **`_is_facade_contamination` を
  engine.generate に実装**し、出力が skill 起動文言のシグネチャ（`エンジンの場所を特定します` /
  `Elevate-Draft-Engine`＋`起動します`）を含んだら汚染として再生成するようにした（テスト3件追加・184件パス）。
  再生成後の press-release は正常なプレスリリース（2023字）で評価 0.268 になった。
- **評価の揺れ**: 昇華版 press-release は別実行で 0.684 と 0.525 の2値を返した（実API評価は確率的。n=1 の注意）。
- n=6 で小さい。旧テーマ（海）では素の生成勝利が1件観測されたが、テーマ変更の再実行後は解消しており、現在の6件は全て昇華勝利。優位性の立証は引き続き `compare --runs N`（n≥10）で行う。

## 2.2.0 (2026-08-09) — 前提知識の注入

**問題**: タスクに付随する素材・制約・背景情報（材料、ターゲット層、価格帯、資料等）を
パイプラインに渡す構造的な仕組みがなく、エージェントが暗黙の前提で捏造していた。

**対処**: 起動時に前提知識の有無を指定し、ある場合はそれを生成の土台として全段階に注入する。
fmt（形の制約）と対になる**内容の制約**であり、直交する。

- CLI に相互排他の `--knowledge 'TEXT'` / `--knowledge-file PATH` / `--ask-knowledge`（対話入力）を追加
- `_resolve_knowledge()` が解決し、`_save_knowledge()` が `--out/knowledge.md` に保存（input.md / format.md と並列）
- diverge / aufheben / finalize / generate の全生成段階にタスク直後（fmt の形式指示より前）として注入
- extract_format には注入しない（形の抽出はタスクのみ。知識を混ぜると抽出精度が下がる）
- 5軸評価には注入しない（ポリシー固定）
- improve の改修ラウンドは diverge が内部で知識を付与するため永続
- compare はベースライン generate と elevated の両方に注入して公平
- テスト test_knowledge.py 19件追加。テスト167件パス

## 2.1.0 (2026-08-09) — 出力フォーマット認識

**問題**: パイプラインは従来、出力形式を「分析レポート（テーゼ草案 → TVRO 最終化）」に固定していた。
そのため「キャッチコピーを開発せよ」というタスクが Target/Value/Risk/Opportunity の分析レポートになり、
5字のタグラインは固定下限 `ELEVATED_MIN_LENGTH=300` で「分量不足」として再生成され続けた。

**対処**: 分野の数だけフォーマットを事前定義するのでなく、パイプライン開始前に
タスクから期待される出力形式を LLM が動的に抽出する（`extract_format`。1回の軽量コール、温度0.0、同一タスクはキャッシュ）。

- `OutputFormat`（deliverable_type / description / draft_guidance / finalize_guidance / min&max_output_length / output_is_direct）を全段階に注入
- `--output-format '<JSON>'` で抽出をスキップして明示指定可能（mock でも有効）
- `--out/format.md` に仕様保存
- 完全性ガードはタスク固有の長さ範囲で判定。直接成果物（output_is_direct）は文末記号を要求しない
- 抽出失敗は既存挙動（分析レポート前提・固定値）にフォールバック
- 仕様の自己矛盾は安全弁で吸収（上限回数の再生成後、構造的に完成した最後の試行を採用）
- agents/*.md・evaluator.py は不変。テスト144件パス
- 実API検証: 歌詞ケース exit 0（lyrics/ の「潮は返る」）

## 2.0.1 (2026-08-09) — improve に高品位停止を追加

improve 実測で「既に高い成果物ほど改修で壊れやすい」が観測された（story-plot: round1 0.860 → round2 0.520 過修正で後退）。

- `--evaluate` 時は overall が `--quality-ceiling`（既定 0.85）以上なら改修ラウンドを生成せず停止
- 従来の `--min-improve`（直前ラウンドとの差）による頭打ち停止と並立
- 停止理由は `progress.md` の「停止理由」欄に記録

## 2.0.0 (2026-08-08) — 機構衣替え（統合→Aufheben）

中核機構を「論理的な統合（synthesis）」から「弁証法的昇華（Aufheben）」へ根本変更。
ユーザー指示「これは用語の変更ではない、仕組みの衣替えだ」。

- DIVERGE → AUFHEBEN → FINALIZE の3段構え
- アウフヘーベン三契機 = 否定（各草案の一面性を否定）/ 保存（真理の契機を継承）/ 高次化（矛盾を包括する新枠組み）
- `DRAFT_TEMPERATURE` は 0.7→0.9（発散と昇華推理の両方に効く）
- `RECONCILIATION_SYSTEM`→`AUFHEBEN_SYSTEM` 等にリネーム（`synthesize_with_reconciliation` メソッド名・CLI `--method two-stage` は維持）

## 1.x (2026-08-08) — 基盤構築期

### 1.3.0 — インストーラとファサード skill
`install.sh`（agents + skill を symlink 設置）、`skills/elevate-draft-engine/SKILL.md`（ファサード。オーケストレーションは Python エンジンに委譲）。

### 1.2.0 — 評価軸のポリシー密着化
5軸をゼロベース再設計（多様性 / 統合性 / 超越性 / 誠実性 / 実用性・均等0.20）。
旧 Quality/Logic/Creativity/Value/Risk から置き換え。Risk 軸は「正直なリスク開示ほど評価が下がる」構造的バイアスのため廃止（反転ロジックも撤廃）。

### 1.1.0 — improve と compare
`improve`（統合版→改修草案→統合の反復）と `compare`（素の生成 vs 統合の実測）を追加。

### 1.0.0 — 初版
DIVERGE（8エージェント草案生成）→ SYNTHESIZE（統合）の2段構え。`agents/*.md` 正本・`main.py` CLI・`elevate/engine.py`。

---

## 機構の進化（履歴）

### 草案のテーゼ集中化

**問題（2026-08-09）**: 草案が「フリー分析レポート」化（1本12〜19KB）して昇華が過剰包摂で崩れる
（2体0.80→4体0.60の実測）＋ 生成が遅すぎる。

**対処**: 草案を先鋭化した1つのテーゼ（核心的主張3文 / 根拠3点各1文 / 前提1文、500〜800字）に制約。
`DRAFT_MAX_LENGTH=1000`（超過は不完全扱いで再生成）。`AUFHEBEN_MIN_LENGTH=60`。
agents/*.md の「草案の作り方」を全面書き換え。

### ベースライン中立化

**問題（2026-08-08）**: 衣替えで ANALYSIS_SYSTEM が「複数の独立した視点が交差する地点から…」と
複数視点を誘引するようになり、素の生成が内部で評議会を演じる（存在しないエージェント名を捏造）。

**対処**: ANALYSIS_SYSTEM を中立化（「具体的・深い洞察を伴い、実行への手がかりを持つ分析」に）。

### per-agent エラー捕捉

**問題（2026-08-09）**: 歌詞の draft_humanist.md（1116字 > DRAFT_MAX_LENGTH=1000）の3回連続超過で
RuntimeError → 未捕捉で行列全体が exit 1 で落ちた。

**対処**: `engine.diverge` に `on_error(name, exc)` を追加。単一エージェントの失敗は部分草案を削除し、
残りで継続。main.py の全コールサイトに `_report_draft_error` を配線。

### elevated のサイズ制約

**問題（2026-08-09）**: 草案のテーゼ集中化後も最終成果物が7000字級のまま残った。

**対処**: `ELEVATED_MAX_LENGTH=1500`（上限・報告書化/過剰包摂の防止）＋ `ELEVATED_MIN_LENGTH=300`
（下限・結論としての分量不足）。`_elevated_is_complete` を最終化・単発昇華・論理検査に配線。

### compare の累積化

**問題（2026-08-09）**: 独立 run では改善連鎖が測れない。

**対処**: run_02+ は `_revision_task` で前回の昇華版を埋め込んだ草案から昇華する改善連鎖に変更。
ベースライン（素の生成/best-of-n）は毎回オリジナルタスクから単発のまま。

### 創作系タスクの草案上限緩和

**問題（2026-08-09）**: 歌詞1116字が DRAFT_MAX_LENGTH=1000 を3回超過して行列が落ちた。

**対処**: `DRAFT_MAX_LENGTH_CREATIVE=3000` ＋ `_is_creative_task(task)` のキーワード法
（歌詞/歌/曲/小説/物語/詩/ポエム/脚本/演劇/絵本/キャッチコピー/ストーリー/プロット/短編）。

### 出力のカテゴリ分類

**問題（2026-08-09）**: 出力がフラットで見通しが悪い。

**対処**: run_NN/ round_NN/ 内を drafts/（draft_*.md）・evaluations/（evaluation_*.md）・
artifacts/（reconciliation/elevated/raw/best_single）の3分類に変更。

### ストリーム草案保存

**対処（2026-08-08）**: `diverge(draft_dir=...)` が各草案を生成前に空の `draft_{name}.md` として作成し、
生成中に逐次追記する。打ち切り再生成時はファイルを空に戻す。

---

## ルーブリック再調整の経緯

**旧ルーブリック（2026-08-08 以前）**: 「明確・一貫・使用可能」を 0.8-1.0 の最上位帯域に置いていた。
凡庸な単発生成でも 0.758（Pass）に張り付き、統合版の向上が可視化できなかった。

### 再調整(1) — 0.5=普通への再アンカー（2026-08-08）
`evaluation/evaluator.py` の採点基準を「0.5 = 普通」に再アンカーし、凡庸な出力は 0.5〜0.6、
良い出力は 0.7〜0.8、卓越した出力のみ 0.9 以上とした。

### 再調整(2) — 「凡庸すぎる」基準の修正（2026-08-08）
再調整(1)には残る問題があった。「0.5=普通」と Quality 0.7-0.8 帯の記述「平凡」がほぼ同義で矛盾し、
凡庸な出力が 0.7 台に張り付く構造が残っていた。0.7-0.8 を「無難でなく確かに良い」、
0.9+ を「凡庸な生成にはない固有の枠組み・見方の転換」に再定義（帯域は変えず記述のみ）。

### 再調整(3) — 5軸のポリシー密着化（2026-08-08）
5軸の「何を測るか」自体を再考。旧 Quality/Logic/Creativity/Value/Risk を廃止し、
本エンジンのポリシー（複数の独立した視点を統合して単一視点を超える）を測る新5軸
（多様性 / 統合性 / 超越性 / 誠実性 / 実用性・均等0.20）に再設計。
Risk 軸は構造的バイアス（正直なリスク開示ほど評価が下がる）のため廃止。
モックの改善推移は 0.610→0.718 から 0.600→0.720 に変わった。

---

## 旧実測記録

**以下の記録はすべて参考であり、事実を主張するものではない。**
機構・ルーブリック・ベースラインは測定後に変更されており、いずれの数値も現在のシステムの
性能を表さない。現在の性能の根拠になり得るのは、現在の機構・現在の基準で `compare` により
実測したデータだけである。

### knowledge-search（2026-08-08, n=1, claude-code, 8エージェント）

**旧ルーブリックで測定した参考記録であり、新基準（再調整(3)後）と直接比較してはならない。**

| 対象 | overall | 内訳 |
|---|---|---|
| 素の生成（単発） | **0.758** | quality 0.85 / logic 0.80 / creativity 0.85 / value 0.80 / risk 0.85 |
| ELEVATE | **0.755** | quality 0.85 / logic 0.75 / creativity 0.95 / value 0.75 / risk 0.85 |
| 差 | **-0.003** | — |

n=1 のため差は統計的に無意味。観察された傾向: ELEVATE の creativity 高（+0.10）に対して
logic・value 低（-0.05）。統合成果物が単発生成にない枠組み（記憶層／ゾンビ知識）へ再構成された
独自性の代償と読める（creativity/logic/value は旧5軸の軸名）。

---

## 知恵の評議会による評価（2026-08-08）

知恵の評議会 full 合議（10体の評価者）が本エンジンを評価し、分類 **discovery_target**
（現在価値 54.5 / 潜在価値 60.3）を得た。評価者全員が「装置の完成度は名手級、効能（昇華優位）は
未実証」で一致。

> **「装置は磨かれているが、効能は未処方。」**

報告: `wisdom-council-layer/examples/digital-elevate-draft-engine/`

### 4軸改訂（実装済み）

| 軸 | 改訂 |
|---|---|
| ①昇華優位性の実証 | `compare --runs N` の集計に勝率 95%CI（Wilson）・差の t 信頼区間・効果量（Cohen's d）を追加。n≥10 の実測で立証する装置へ |
| ②具体性保存指数 | `evaluation/specificity.py` で草案の固有名詞・数字・独自造語が昇華後どれだけ残るかを実測 |
| ③温度近似の誤差定量 | `calibrate` サブコマンドで sdk / claude-code の出力分散を定量化 |
| ④logic軸の後退への収束 | `--logic-check` で最終化後に論理一貫性の復元工程を適用 |

### preserve（失ってはならないもの）

誠実さの設計（未実証の明記・勝率開示）、自己検証の体現、ゾンビ知識・組織の記憶層・
0件を心臓に置く設計、humanist の「見捨てられた人」という感情・意味の核。
感情の道具化ガード `_detect_sentimentality` は soft guard として CLI 警告（生成は止めない）。
